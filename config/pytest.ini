[pytest]
# ⚠️ 本文件不在项目根 → 在项目根执行时需显式指定：`python -m pytest -c config/pytest.ini test/ -q`
#    （2026-09-13 目录重排时由项目根移入 config/；testpaths 相对调用目录解析，已实测可用）
# 测试根目录与标记注册（selenium_legacy 相关用例默认不收集，见 pytest -m selenium）
testpaths = test
markers =
    selenium: 需要 Chrome/chromedriver 的保底引擎用例（默认不参与 CI）
