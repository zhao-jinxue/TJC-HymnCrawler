# 修复歌词采集缺陷：副歌（lyrics_chorus）整段丢失

- 日期：2026-09-12
- 工具链：Cline
- 涉及模块：`crawler_core/extractor.py`、`crawler_core/db.py`、（拟新增）歌词 API 采集路径

## 任务 1：定位并修复歌词采集丢失副歌的缺陷

### 提问

1. 用户给出诗歌详情页 `https://sacredmusic.tjc.org.tw/hymn/12`（第 12 首〈耶穌尊名〉）。
2. 追问指出：DB 中 `verse_1` 只有 2 行，而网页上该节显示 4 行——网页 HTML 中同一节由
   两个 `div.lyrics_box` 组成（前者为节歌词，后者为副歌），说明「歌词的下载还存在缺陷，需要修复」。

### 解决思路

**根因定位**（用项目自带 Selenium 驱动导出真实 DOM）：

```text
hymn/12  tabs = [第1節, 第2節, 第3節, 第4節]
  tab[0] boxes=2 -> ['耶穌尊名入我耳中…', '美哉，大哉，耶穌我主！…']
  tab[1] boxes=2 -> ['耶穌尊名至高、至榮…', '美哉，大哉，耶穌我主！…']   ← 第 2 个 box 每节重复
  ...
```

`crawler_core/extractor.py` 的歌词循环中：

```python
boxes = driver.find_elements(By.CSS_SELECTOR, ".lyrics_box")
for box in boxes:
    text = box.text.strip()
    if text and text not in lyrics_parts:
        lyrics_parts.append(text)
        break        # ← 每个 Tab 只取第一个 box，副歌被丢弃
```

**影响面量化**（比对官网 JSON API `GET /api/hymn/{no}`，474 首逐首核对，脚本只读）：

| 项 | 数值 |
| --- | --- |
| 有副歌（`lyrics_chorus` 非空）的诗歌 | **270 首** |
| 其中副歌已进 DB 的 | **0 首**（100% 丢失，合计 559 行文本） |
| 节数一致 | 469 首 |
| 歌词错位/缺行 | 275（缺 16 行）、349（缺 4 行） |
| API 未取到（网络抖动，可重试） | 57 / 121 / 394 |

**可选方案**：

- A：DOM 方案——修正为收集每个 Tab 下全部 `.lyrics_box`，副歌并入每节末尾。
- B：API 方案——歌词改取 `GET /api/hymn/{no}` 的 `lyrics[]` + `lyrics_chorus`，正歌与副歌分别落库。

**选定方案（用户确认）**：方案 A/B 合并为「以官方 API 为权威源」——
歌词改取 `GET /api/hymn/{no}` 的 `lyrics[]`（正歌）与 `lyrics_chorus`（副歌），
正歌存入 `verse_1..10`，副歌新增 DB v6 字段 `chorus` 单独存放（与官网数据模型一致）；
同时保留 Selenium DOM 路径作为 API 不可用时的兜底，并修掉 `break` 缺陷。

### 最终结果

**完成结论**：缺陷已修复并全量重抓校验通过。
`https://sacredmusic.tjc.org.tw/hymn/12` 现落库为：`verse_1` = 正歌 2 行、
`chorus` = 副歌 2 行（即网页上同一 Tab 的两个 `lyrics_box`），不再丢内容。

**改动文件清单**

| 文件 | 改动 |
| --- | --- |
| `crawler_core/lyrics_api.py` | **新增**：官网 API 取词（`fetch_hymn_lyrics`）、批量刷新（`backfill`/`run`）、断点进度、`_update_row` |
| `crawler_core/config.py` | 新增 `API_HYMN_URL` |
| `crawler_core/db.py` | DB v6：新增 `chorus` 字段（`ensure_chorus_field` 幂等迁移）、建表/`save_to_db` UPSERT 支持副歌（空值保留旧值）、状态打印增加「有副歌」 |
| `crawler_core/extractor.py` | 歌词改为「API 优先 + DOM 兜底」；新增纯函数 `group_lyrics_boxes` 归并多个 `.lyrics_box`（副歌按「各 Tab 重复出现的尾部 box」判定，杜绝丢文本） |
| `crawler_fast.py` | 菜单新增 `9` 歌词重抓（选项范围 0-9） |
| `test/test_lyrics_api.py` | **新增** 17 个单测：box 归并、文本归一、API 重试/降级（monkeypatch 无网络）、v6 迁移与写库、断点进度 |
| `README.md` / `docs/SESSION_SUMMARY.md` / `.gitignore` | 文档与忽略项同步（`lyrics_progress.json`） |

**验证方式与结果**

1. 全量重抓：`run(force=True, reset=True)` → `🏁 成功 474 | 跳过 0 | 失败 0 | 耗时 639.7s`，
   副歌覆盖 `270/474`（与修复前 API 普查的 270 首完全一致）。
2. 逐首比对（脚本 `/tmp/verify_chorus.py`，官网 API 为基准）：474 首
   **正歌 0 不一致、副歌 0 不一致、节数 0 不一致**；`download_status` / `integrity_status`
   与资源路径字段均未被误改。
3. 个案复核：275（原把副歌当第 5 节）现 4 节且副歌随节内文本保存（官网 `lyrics_chorus` 为空）；
   349（原仅 2 节）现 4 节 + 副歌入库；12 首 `chorus` 与网页副歌逐字一致。
4. 质量门禁：`pytest test/ -x -q` 23 passed；`ruff check crawler_core/ test/test_lyrics_api.py crawler_fast.py` 全通过；
   `bandit -c bandit.yaml -r crawler_core` 0 issue；`mypy crawler_core` 无问题。

**遇到的困难**

- 站点证书链缺 Subject Key Identifier，Python 3.14 下 `requests` 默认校验失败 → 沿用项目既有做法
  `verify=False` + `# nosec B501`（与 `downloader.py` / `probe.py` 一致）。
- 服务端 HTML 里 `.lyrics_box` 是空占位（Vue 客户端渲染），直接 requests 抓 HTML 拿不到歌词 →
  改用官方 JSON API，并保留 Selenium DOM 兜底。
- bandit 要求 `# nosec` 与告警同一行，首版写在上一行导致仍是 High 告警，已修正。

**遗留问题**

- 本次仅重抓歌词；`title` / `lyricist` / `composer` / `source_info` 仍走 DOM，未与 API 的
  `lyricists` / `composers` / `history` / `youtube_urls` 字段对账（如需可另开任务）。
- 官网 API 存在偶发超时（首轮普查 3 首、本轮 0 首），已内置重试 + 失败复核一轮；
  如长期使用建议在 `verify` 中增加歌词一致性抽查。
- `chorus` 只在 `save_to_db`（Step 2）与 `lyrics_api` 中维护，导出类脚本（`tool/`）未涉及该字段。

## 任务 2：评估「用官网 JSON API 重构、替代 Selenium」并输出设计文档

### 提问

- 用户：官网 JSON API 感觉更好用，是否可以用此方式**重构项目代码、替代 Selenium 的结构**，实现更快的爬虫效果？
- 追问（用户选择）：**先别动代码**，把完整重构方案写成设计文档，审阅后再决定。

### 解决思路

**方针：不做"感觉可行"的评估，全部结论用实测数据支撑；本轮只读、零代码改动。**

1. **摸清 Selenium 使用面**：全文检索确认只有 4 处使用——`driver.py`（定义）、`scanner.py`（列表翻页）、
   `extractor.py`（详情 + DOM 兜底）、`probe.py`（音频点击播放）；`downloader/images/checksums/verify` 下游均不用。
2. **摸清 API 全貌**：`/api/hymn/12` 已知；进一步发现 `/api/hymn` 是**分页列表接口**，
   且列表项字段 = 详情接口全字段（逐字段比对 5 首：值不同 0 个，仅详情多 `prev_no/next_no`）
   → **48 个请求（2.2 MB）即可拿全 474 首**，Step 1 与 Step 2 可合并。
3. **覆盖率对账（474 首逐首）**：PDF 与 `probe_report` **零差异**（直链 HEAD 200 `application/pdf`）；
   音频 API 474 首/1130 条 vs 现有 473 首/1120 条 → **API 反而多 11 首**（Selenium 点击式探测漏抓，
   如 122「彼此相愛」完全没抓到）；歌词/副歌与现状逐首 0 不一致。
4. **速度基准（同机实测）**：详情 2.27 s/首（Selenium）vs 0.212 s/首（API 10 线程）；
   列表 2.76 s/页 vs 0.61 s；音频探测从 8–10 min 降为 0；
   全链路由 **≈28–30 min → ≈2–3 min（≈10×）**。
5. **兼容性验证（决定"零迁移"能否成立）**：
   - API 顺序 == 现有 `seq` 序号：**474/474 一致**
   - 目录名 == `f"{seq:03d}_{sanitize(no+name)}"`：**473/474**（唯一例外 349，官网标题已改）
   - 文件名规则（`{no}_五线谱.pdf` / `{no}_{版本}版.{ext}`）：抽查全部命中
   - 音频版本名映射 = `API 分类名 + "版"`（与现有 1120 条文件名一致）
6. **风险识别**：内部接口非公开承诺（需字段自检 + DOM 降级）、限流（实测 16 线程 11 req/s 无 429，
   仍需并发上限 + 退避重试）、349 标题漂移（目录名以既有 `url_map.txt` 为准，绝不重命名）。

**纠错记录**：首轮"目录名可推导性"检查用了 `re.sub(r'[^0-9A-Za-z _-]','',…)`，
  会把中文全部剔除，导致校验退化为恒真（误报 474/474 通过）；改用与 `Scanner._create_dir`
  完全一致的 sanitize 规则后真实结果为 **473/474**（32 条差异实为标点被剔除，如「21美哉，主耶穌」）。

### 最终结果

**改了什么文件**

- 新增 `docs/API_REFACTOR_PLAN.md`（458 行）：0 TL;DR / 1 背景与目标 / 2 现状架构与 Selenium 使用点 /
  3 取证结果（API 全貌、474 首对账、列表=详情、速度实测、兼容性验证）/ 4 目标架构（数据流图、模块改动总表、
  `api_client.py` 接口草案）/ 5 详细设计（字段映射表、各模块改造、DB v7 迁移、增量算法、菜单变更）/
  6 兼容性与迁移 + 回滚方案 / 7 分阶段计划 P0(3–4h)→P1(2–3h)→P2(1h) 与验收标准 / 8 测试计划 /
  9 风险与对策 / 10 待决策项（6 项）/ 附录 A 取证脚本清单 / 附录 B 关键样例
- 本会话日志追加任务 2 记录

**验证了什么**

- 全部结论均有实测支撑：48 页全量拉取（474 首 / 失败 0 页）、474 首字段覆盖率统计、
  与 DB/`probe_report.json` 逐首对账、PDF 直链 HEAD 实测 200、并发限流测试（16 线程无 429）、
  基准测试（Selenium vs API）、目录名/文件名/顺序一致性验证
- 文档自检：无工具标签污染、代码块 22 个（成对）、章节结构完整

**遗留什么**

- 本轮**未改任何代码**，待用户评审 `docs/API_REFACTOR_PLAN.md` 第 10 章 6 个待决策项后开工
- 取证脚本留在 `/tmp`（只读），未纳入仓库；如需回归基准可后续落到 `tool/`
- 已知数据质量差异待修（17 首 `composer=Unknown`、349 标题/词作者、11 首缺失音频、`hymn_category` 重建）
- 任务 1 的遗留项（`tool/` 导出脚本未含 `chorus`、`verify` 未抽查歌词）仍未处理


## 任务 3：澄清「音频有链接但下载失败」的处理，并按建议设计 Selenium 保底目录

### 提问
- 如果诗歌的音频下载失败（例如 62 首诗歌，人声版有链接但无法下载），是怎么处理的？改用 API 后遇到这类异常怎么处理，是否会导致程序崩溃？
- 可以创建一个 Selenium 版本的程序目录，把现有 Selenium 结构的代码迁移过去，主代码用 API 重构——既有保底，又有新方法。

### 解决思路
1. **先打通单条坏链的全链证据**：把 62 号从「API → probe_report → DB → 本地文件 → HTTP 实测」逐环取证，
   弄清"有链接但下载不了"的真实后果（是否崩溃、状态如何、是否可恢复）；
2. **再做全量可达性实测**：对 API 给出的全部 2078 个资源 URL（948 PDF + 1130 音频）做可达性探测，
   并用「重试 + GET Range 兜底」复测，把**瞬时网络抖动**与**真死链**分开——不能拿"有链接"当"能下载"；
3. **用实测口径重做音频对账**：以「实测 200 的 URL」对比「本地实有文件」逐首逐版本对账，校准初轮"API 多 11 首"之说；
4. **异常处理设计**：四层容错（URL 级 / 记录级 / 阶段级 / 进程级）+ 资源可用性状态机；
   关键约束是**对现有 `probe_report.json` 结构零破坏**（复用"下划线前缀键会被现有代码过滤"这一事实）；
5. **保底方案采纳用户建议**：Selenium 实现整体迁入 `crawler_core/legacy/`，主包仅保留转发层与引擎开关，
   `selenium` 移出主依赖；两引擎共用同一命名模块，防止规则漂移。

### 关键事实（本次实测，均为只读）
- **#62 人聲版**：API 返回的 URL 与 Selenium 抓到的**逐字符相同**（`…/audio/0dcc19451da297d5a15105d3e7eea85d.m4a`）→ HTTP **404**，
  属服务端文件缺失；换 API **不会自动修好**，也不比 Selenium 更差。
- **现状不会崩溃**：`download_one()` 对非 200 与任何异常一律 `return (item, False)`，主循环仅 `fail += 1` 后 continue；
  随后 `_update_download_status` 得 `partial(3/4)`、`_verify_and_sync` 得 `integrity_status=failed`、
  `_backfill_paths_to_db` 只写本地存在的版本（坏 URL 不入库）、`verify.py` 报缺失并输出**硬编码**的 `if h == "62"` 归档说明。
- **全量音频 1130 条**：实测可用 **1119**；不可用 **10** = 9 条 `file_url`/`file` 同时为 `null` 的空记录
  （#178、#249、#255、#268、#274_b、#308、#386、#387、#389）+ #62 的 404。
- **2078 个资源 URL**：首轮 34 条非 200（ReadTimeout 13、SSLError 5、MissingSchema 9、404 1），
  复测后 **33 条恢复 200**（瞬时失败率 ≈1.6%）→ 换 API 后**必须带重试**，否则会静默丢数据。
- **本地比 API 多 138 条**：34 首 × 4 条 `合唱-N部版` 与 `四部合唱-N部版` **字节数 + md5 完全相同**（历史重复副本，**145.7 MB**）
  + #349 的 `人聲版`/`四部合唱版`（官网已下架）。→ 所谓"多出的 11 首"多数是空/坏记录，**真正可补只有 #201 人聲版 1 条**。
- **#201 人聲版** URL 后缀为 `.mp4`（实测 200 `video/mp4`、2 489 768 B、头部 `ftyp`）→ 必须归一到 `.m4a`，
  否则 `verify.py` 的 `RESOURCE_EXTS={.pdf,.m4a,.mp3}` 会漏统计。
- **`seq` ≠ `hymn_number`**：423/474 条不相等（5 对甲乙编号使 474 个条目只对应 469 个编号）
  → **目录名必须用列表位置 `seq`，资源文件名用诗歌编号 `no`**，实现中不得混用。

### 最终结果
- **改动文件（仅文档）**：`docs/API_REFACTOR_PLAN.md` 升级到 v1.1
  - §0 TL;DR：校准音频口径、新增"运行依赖/失败处理"两行、加 v1.1 校准提示框
  - §1.1：新增痛点 5「失败处理粗糙」（无预检 / 失败不落盘 / 无重试 / 归档硬编码）
  - §3.2：音频对账改为「列出 vs **实测可下载**」双口径 + 两个说明框（音频二次校准、`seq≠no`）
  - **新增 §3.6**「资源 URL 可下载性全量实测（2078 个 URL）」、**新增 §3.7**「历史重复文件 145.7 MB」
  - §4.2：新增 `naming.py`、`legacy/` 两行；`driver.py` 改为转发层
  - **新增 §4.4**「双引擎与 Selenium 保底目录」：目录树、引擎开关、依赖策略、兼容策略、测试策略
  - §5.2/§5.4/§5.5：补 `seq`/`no` 区分、URL 预检、`.mp4` 归一化、`legacy` 依赖策略
  - **新增 §5.9**「异常与失败处理设计」：#62 现状回放表、四层容错、资源可用性状态机、参数默认值、四条"不崩断言"
  - §7：三阶段验收更新（新增"不崩断言全通过""`--engine api` 下不含 selenium""legacy 保底可用"）
  - §9：风险表新增 4 行（瞬时抖动 / 空记录坏链 / 存量重复 / `.mp4` 后缀）
  - §10：决策项由 6 项扩到 **9 项**（①改为"移入 legacy 保底不删除"；⑥音频补齐改为 1 条；新增重复文件、状态语义、后缀归一化）
  - 附录 A：新增本轮 7 个取证脚本清单
- **未改动任何项目代码**；取证脚本仍留在 `/tmp`（只读），未纳入仓库
- **验证**：本轮全部为只读实测（未运行 pytest，因代码零改动）；已复核文档内容，无工具标签残留
- **遗留**：§10 的 9 个决策项仍待用户拍板；取证脚本是否落到 `tool/`（供回归）待定



## 任务 4：§10 决策拍板落地——数据对账确认 + 数据清理/迁移（API 重构 P0 前置）

### 提问
1. Selenium 保底包命名用 `selenium_legacy`
2. 需要 `crawler_selenium.py` 作为独立备选入口
3. #349：新建「354_349奇妙的耶穌」并迁移文件、数据库同步修改
4. 音频补齐范围：补 #201 那 1 条
5. DB v7 字段形态：加一列 `api_raw`
6. `hymn_category`：用 API 重建整表
7. 缓存策略：48 页响应落盘 `Hymn_Downloads/api_cache/`，不 gitignore（2.2 MB 入库可接受）
8. 存量重复文件：**先与官网数据对账**，确认是重复文件那就删除
9. 不可用资源的 `download_status` 语义：采用推荐方式
10. 同意 `.mp4 → .m4a` 归一化（前提：不影响播放）

### 解决思路
- 执行顺序定为「**先对账取证 → 再动数据 → 最后进 P0**」：⑧ 是破坏性操作，必须先拿到站点侧证据链
- ⑧ 的判据设计（回答"是否本来就是多个版本的合唱"）：
  1. 以 **API 为站点唯一真源**，统计 474 首的 `audio_category.name` 分布；
  2. 对 34 首成对文件做**全文件 md5** 比对（放弃此前的前缀 md5/体积近似）；
  3. 额外核对这些文件**是否被产物引用**（`probe_report.json` / DB `audio_versions` / `checksums.json` / 期望集合）
  - 三条同时成立（站点无此分类 + 逐字节相同 + 无任何引用）→ 判定为"分类改名遗留的孤儿副本"，删除安全
- ④ 先验证"是否真的缺"：本地文件与 API URL 内容做 md5 比对，再决定是否下载（避免无谓重下 2.5 MB）
- ③ 迁移范围一次找全：目录 + `url_map.txt` + `probe_report.json` + `step5_progress.json` + DB 所有路径列 + `checksums.json`（相对名不变）；改动前 `grep` 全仓定位引用点
- 清理动作全部脚本化 + 守卫：删除前 `assert md5(a)==md5(b)`；DB 按 `PRAGMA table_info` 全列扫描替换，避免漏改字段

### 最终结果
- **对账证据（⑧ 的答案：不是多版本，是历史重复文件）**
  - API 分类分布：`四部合唱-1..4部` 各 **34 首**；`合唱-1..4部` **仅 1 首（#6「頌主造化大功」）** → 那 34 首在站点侧根本没有「合唱-N部」分类
  - 136 对文件（34 首 × 4 部）**全文件 md5 完全相同**（差异 0 对）；抽样与全量两轮一致
  - 引用核对：136 个文件**均未被** `probe_report.json` / DB / 期望集合引用（只有 #6 的 4 个被引用）
  - → 结论：站点分类名「合唱-N部 → 四部合唱-N部」改名后，两轮 Selenium 采集各存一份所致，删除不影响任何产物
- **已执行的数据操作（3 项）**
  1. **删除 136 个重复文件**（`{no}_合唱-N部版.{ext}`），释放 **134.7 MB**（全文件求和实测；早前 145.7 MB 为估算值，已修正）；同步 34 个目录的 `checksums.json`
  2. **#201 `.mp4 → .m4a` 归一化**：先验证"本地与远端 md5 一致（`c6b9c8fce636…`）→ **无需补下载**"，再改名 + `checksums.json` + `probe_report.ext`（保留 `_src_ext`）+ DB 路径，四处同步
  3. **#349 目录迁移**：`354_349救主正在等候` → `354_349奇妙的耶穌`（目录 + `url_map.txt` + `probe_report.title` + `step5_progress.json` + DB **6 个字段**：`title`/`staff_img_path`/`numbered_img_path`/`audio_versions`/`staff_png_path`/`numbered_png_path`）
- **终版数据口径**：本地音频 **1121 条** = API 实测可用 **1119** + #349 已下架留档 **2**；**缺失 0**；目录名规则 **474/474** 全部符合
- **改动文件**：`docs/API_REFACTOR_PLAN.md`（v1.1 → **v1.2**：§10 改为「决策定稿表」，§3.2/§3.5/§3.7/§4.2/§4.4/§5.1/§5.6/§5.7/§6/§7/§8/§9/附录 A 同步）、`crawler_core/verify.py`（`RESOURCE_EXTS` 加 `.mp4` 兜底）、数据产物（`probe_report.json`、`Hymn_Downloads/url_map.txt`、34 个 `checksums.json`、目录改名、`tjc_hymn.db`）、本日志
- **验证**：`verify.py` → 三方对账 ✅（474/474/474），资源 2068 项完整 2067（唯一缺失仍为 #62 人聲版 404，待 P0 按决策 ⑨ 归正为 `completed + _unavailable`）；`pytest test/ -q` → **23 passed**
- **遗留**：
  - #349 的 2 条已下架文件（`349_人聲版.mp3`、`349_四部合唱版.m4a`）**不是重复文件 → 保留留档**，P1 对账报表标注 `site_removed`
  - #349 官网现仅剩 1 条 `鋼琴` 音频且 URL 已换新（`…af2877e3….mp3`，本地为旧 `…2fbccdb4….m4a`）→ P1 决定是否重下替换
  - §10 十项已全部定稿 → **P0 可开工**（`api_client` / `naming` / `selenium_legacy` 迁移 / `scan_api` / URL 预检 / `api_cache` / `crawler_selenium.py`）

- 本次共处理 4 个任务：
  1. 修复歌词副歌（`lyrics_chorus`）整段丢失缺陷（已实现、已全量重抓、已提交）
  2. 评估「用官网 JSON API 重构替代 Selenium」并输出设计文档 `docs/API_REFACTOR_PLAN.md`（待评审，未动手）
  3. 澄清「音频有链接但下载失败」(#62) 的现状处理与 API 版异常设计，并采纳用户建议确定 Selenium 保底目录方案（文档 v1.1）
  4. 用户拍板 §10 十项决策；完成「重复文件对账确认 + 删除、#201 归一化、#349 迁移」三项数据操作（文档 v1.2，P0 就绪）
- 关键收获：
  1. 「抓不到」的根因往往在 `break` 之类的提前退出——站点把一节歌词拆成多个同类元素，
     只取第一个就会静默丢内容，必须做全量元素归并 + 与官方 API 对账
  2. 只要站点存在结构化的官方 API（`/api/hymn/{no}`），优先用它取文本字段，
     比解析 SSR/CSR DOM 更稳、更快、更易校验；本次进一步证实**列表接口即全量详情**，
     全量 474 首只需 48 个请求
  3. 数据修复类任务应先做「全量影响面普查」再动手，避免只修个案
  4. 重构类决策必须以**实测基准 + 逐首对账 + 兼容性验证**为前提（本次 10× 提速、473/474 目录名可推导等结论全部来自实测，而非推测）
  5. 写校验脚本时警惕"恒真式断言"：正则把中文剔除后 `x in y` 会静默退化为永远成立，
     必须用与生产代码一致的规则复算（本次已发现并纠正一处误判）
  6. **"有链接" ≠ "能下载"**：接口给的 URL 必须做可达性预检，并把「瞬时抖动（可重试）」与「真缺失（4xx/空值，不重试）」
     分类落盘；否则坏链会永久拉低 `download_status`（#62 永远 `partial(3/4)`）
  7. **对账口径要用"实测"而非"字段存在"**：初轮按字段计数得出"API 多 11 首音频"，
     全量可达性实测后发现其中 9 条是空记录、1 条 404，**真正可补仅 1 条**；同时暴露本地 136 个内容重复文件
  8. **统计异常前先用 md5/体积证伪"差异"**：`合唱-N部版` 与 `四部合唱-N部版` 看似两种版本，
     实为同一文件的两份副本；本轮进一步用**全文件 md5 + API 分类分布 + 引用核对**三重证据确认其为"分类改名遗留孤儿文件"，
     删除 136 个（134.7 MB）后三方对账与测试仍全绿
  9. 兼容性设计可以"零破坏"：新增字段一律挂下划线前缀键，现有代码已过滤 `_` 键，
     对旧消费者完全透明——这是本次异常状态落盘方案能不动 `downloader.py` 的原因
 10. **"缺文件"结论必须复跑一次内容比对**：#201 曾被判"仅 1 条可补（2.5 MB）"，
     实际本地早已存在且 md5 与远端一致——误判根因是统计脚本 `RESOURCE_EXTS` 不含 `.mp4`（本地文件对统计"隐身"）；
     归一化后缀既修了统计，也修了命名一致性
 11. **破坏性数据操作要先立"三条判据"**：站点真源无该分类 + 逐字节相同 + 无任何产物引用，
     三者齐备才删；删除前 `assert md5` 守卫、删除后跑一次全量校验（本次删除 136 文件后 474/474 对账仍一致）
 12. **改目录名是一次"全链替换"**：目录、`url_map.txt`、`probe_report.json`、`step5_progress.json`、
     DB 所有路径列（含 `*_png_path`）都要改；用 `PRAGMA table_info` 全列扫描替换比手写列名可靠（本次命中 6 列）
- 遗留事项：
  - 任务 1：元数据未与 API 对账、`verify` 未抽查歌词、`tool/` 导出脚本未含 `chorus`
  - 任务 3/4：#349 仍有 2 条已下架文件留档、其 `鋼琴` 音频 URL 已换新待 P1 决定是否替换；
    取证/审计脚本仍只在 `/tmp`，待按 §8 落入 `tool/data_audit/`
  - 任务 4：**§10 十项决策全部定稿 → P0 可开工**（预计 3–4 h）

---

## 任务 5：实施 API 重构 P0/P1/P2（API 主路径 + Selenium 保底 + 数据模型/增量 + 瘦身）

### 提问
> 「docs/API_REFACTOR_PLAN.md，设计已经评审完成，计划没有问题，继续执行，直到完成预期目标」

（即：按 v1.2 方案执行 P0 → P1 → P2，直到 §7 验收标准全部达成。）

### 解决思路
1. **先解决"命名与映射"这两类唯一真源**，再改流程：
   - 新增 `naming.py`（目录名/文件名/版本名/后缀归一化），双引擎共用 → 杜绝历史「合唱-N部版 vs 四部合唱-N部版」命名漂移；
   - 新增 `api_client.py`（请求退避重试 + 48 页磁盘缓存 + 纯函数字段映射 + 资源可用性状态机）。
2. **可用性状态机**（§5.9.3）落地方式选"零破坏"实现：不可用资源把 `url` 置 `None`，
   原 URL 存入 `_url`、状态存 `_http_status`、原因存 `_unavailable` → 下游 `downloader`/`verify`
   的 `info.get("url")` 判断天然跳过，**`probe_report.json` 结构与旧消费者零改动**。
3. **保底引擎**按 §4.4：旧实现整体搬入 `crawler_core/selenium_legacy/`（只搬不改），
   `driver.py` 收敛为转发层，selenium 全部延迟导入 → `--engine api` 下 `sys.modules` 不含 selenium。
4. **DB v7 只加一列** `api_raw`（决策⑤），并对 `title/作者/源考` 补"空值不覆盖旧值"守卫，
   避免 API 侧缺失（#25/#31/#66/#299 无 lyricists、#349 无 history）把库内既有值冲成 `Unknown`/空串。
5. **#349 的新发现**（本轮取证）：官网 349 号已由《救主正在等候》换成《奇妙的耶穌》——
   旧直链 `score/sheet|num/349.pdf` 现已 **404**，新文件为哈希命名 URL，且本地旧 PDF 文本确为《救主正在等待》
   → 按"站点当前内容为准 + 历史零删除"处理：旧 5 个资源移入 `354_349奇妙的耶穌/_archive/`（含 md5 说明），
   再按 API 当前 URL 重新下载 3 个规范文件。
6. **导航与文档**：`crawler_fast.py` 加 `--engine/--step/--refresh-api-cache` 与菜单 10，
   新增 `crawler_selenium.py` 独立保底入口；`pytest.ini` 注册 `selenium` 标记；`requirements[-selenium].txt` 拆分依赖。

### 最终结果

**改动文件（新增 12 / 修改 12）**

| 类型 | 文件 |
| --- | --- |
| 新增 | `crawler_core/api_client.py`（~660 行：重试/缓存/映射/状态机/`api_raw` 助手）、`crawler_core/naming.py`、`crawler_core/sync.py`（增量）、`crawler_core/selenium_legacy/`（`__init__/driver/scanner_selenium/extractor_dom/probe_audio/README.md`）、`crawler_selenium.py`、`requirements.txt`、`requirements-selenium.txt`、`pytest.ini`、`test/test_api_client.py`、`test/test_db_v7.py`、`test/test_no_crash.py`、`tool/data_audit/`（4 脚本 + README） |
| 修改 | `crawler_core/config.py`（引擎/并发/重试/预检开关，支持环境变量覆盖）、`scanner.py`（`scan_api`/`scan_legacy`/`check_api`）、`extractor.py`（API 并发主路径 + DOM 降级 + `_normalize_engine`）、`probe.py`（API 清单 + URL 预检 + `_unavailable` 汇总 + 保底转发）、`driver.py`（转发层）、`db.py`（v7 `api_raw` / 空值守卫 / `rebuild_hymn_category` / `print_db_status` 增强）、`verify.py`（`_unavailable` 数据驱动归档，删 `h == "62"` 硬编码）、`downloader.py`（瞬时错误退避重试 + `_download_error`）、`lyrics_api.py`（薄封装 api_client）、`crawler_fast.py`（CLI + 菜单 10 + 非 TTY 兜底）、`test/test_lyrics_api.py`（改 patch `api_client`）、`README.md` |

**验收结果（全部实测通过）**

| 验收项（§7） | 结果 |
| --- | --- |
| Step 1 ≤ 15 s 且 `url_map.txt` 474 行完全一致 | ✅ **冷启动 5.9 s / 命中缓存 0.0 s**；md5 `51f69558…` 前后不变 |
| Step 2 全量 474 首 ≤ 3 min，歌词/副歌逐首 0 不一致 | ✅ **58.7 s**，`verse_count/verse_1..10/chorus` **0 处差异**，`api_raw` 覆盖 474/474 |
| `probe_report.json` PDF 0 差异 / 音频可用 1119 / `_unavailable` 恰 10 / 无新增误报 | ✅ 音频 **474 首全有**、可用 **1119**、`_unavailable` **10**（9 空记录 + #62 404）、`site_removed` 0；PDF 仅 **#349 一处真差异**（站点换诗换文件，旧 URL 已 404，已按新 URL 重下并留档旧文件） |
| 不崩断言 4 项、退出码 0 | ✅ `test/test_no_crash.py` 12 项（坏 URL/断网续跑/null URL 不下载/结构改版不崩）+ 额外发现"断网但命中 `api_cache` 仍能完成 Step 2" |
| `--engine api` 下 `sys.modules` 不含 selenium；保底链可跑通 | ✅ 断言通过；`crawler_selenium.py --step 1` 实测 **474 首 / 24 页 / 20.7 s**，`init_driver` 冒烟通过 |
| `api_cache/` 48 JSON、二次命中缓存 | ✅ 48 文件 ≈2.9 MB（已入 git 跟踪），二次 `fetch_all` 0.02 s |
| `pytest` + `ruff` + `bandit` + `mypy` 全绿 | ✅ **81 passed**；ruff `All checks passed`；mypy `Success (22 files)`；bandit 退出码 0 |
| 资源对账 / 下载状态归正（P1） | ✅ `verify.py`：三方 **474/474/474**、资源 **2067/2067 完整（100%）**、失败任务 **无**；`download_status` **completed 474** |
| `hymn_category` API 重建（P1） | ✅ **47 类 / 474 首**，逐类数量与 API 完全一致，幂等复跑一致（注：方案文档原写"45 类"为旧快照数） |
| 增量同步（P1） | ✅ 水位 `W=2026-09-07T05:53:28Z`，二次运行 **变更 0 / 未变 474**；`pending_downloads` 修正为排除预检不可用资源（#62 不再误报） |
| 元数据残留修正（P1） | ✅ `composer` 18 首修正（#25/#31 由 `Unknown` → 真实作者，其余为多作者合并）、`lyricist` 13 首补全多值；`title` 0 变化；`source_info` 420 处**仅空白/HTML 实体差异**（去空白后逐首相同，总字数 312,474 → 309,334，无内容丢失） |

**数据变更**

- DB 迁移至 **v7**（`api_raw` 一列，474/474 覆盖）；`download_status`/`integrity_status` 全量归正为 `completed`/`passed`。
- `#349`：旧 5 个资源（2 PDF + 3 音频）移入 `_archive/`，新下载 `349_五线谱.pdf`（1,022,790 B）/ `349_简谱.pdf`（194,773 B）/ `349_鋼琴版.mp3`（3,074,506 B）。
- `hymn_category` 重建为 API 口径（47 类）。

**遇到的困难**

1. **长命令在工具终端里会被"新一轮命令"打断**：多次导致 Step 2 / 资源探测中途被 kill、且块缓冲使日志为空 → 最终改用
   `setsid nohup python -u … > /tmp/xxx.out 2>&1 &` 后台落盘 + 只读轮询，问题解除。
2. **CLI 参数未生效导致 17 分钟假死**：`main()` 原写法仅在显式传 `argv` 时解析参数，直接运行时 `--step 1` 被忽略并进入交互菜单等待输入。
   已修（`parse_args(None)` 走 `sys.argv`），并新增**非 TTY 兜底**（无 `--step` 直接返回退出码 2，避免 CI/cron 悬停）。
3. **离线测试被缓存"骗过"**：断网用例里 `fetch_all` 命中 `api_cache` 从而"成功"，暴露测试前提错误后改为 `use_cache=False`，
   并把该行为补成一条正向用例（离线复现能力）。
4. **非字符串字段会让映射函数崩栈**（`)`）：`history=123` 触发 `to_source_info` 的 `AttributeError` → 新增 `_text()` 兜底，
   覆盖 `history/no/name/pdf_url/file_url/音频分类名` 等所有站点改版可能变型的入口。

**遗留事项**

- `#201`、`#305`（同名 `鋼琴` 两条，规则=**最后一条为准**并记入 `_duplicates`）、`#122` 等历史个案已在 probe/DB 中归正，无需人工介入。
- `Hymn_Downloads/354_349奇妙的耶穌/_archive/` 为历史留档（5 文件 + README + md5），`verify` 不计入统计；如需彻底清理请人工确认后删除。
- `selenium_legacy/*` 未纳入默认 pytest（无浏览器 CI 也能全绿）；需要时 `pytest -m selenium`。
- `docs/API_REFACTOR_PLAN.md` 的「45 类」等旧快照数字已按实测更新为 47 类。

## 会话总结

- 本次共处理 5 个任务：① 修复副歌丢失；② API 重构可行性评估与方案定稿；③ 失败处理设计 + Selenium 保底布局；
  ④ §10 决策落地（数据对账/清理/迁移）；⑤ **实施 P0/P1+P2 并完成全部 §7 验收**
- 关键收获（本轮新增 13–17，1–12 见任务 4 末尾）：
 13. **「零破坏」状态机可以做到下游零改动**：把不可用资源标成 `url=None`，状态/原因挂下划线键，
     旧代码 `info.get("url")` 即天然跳过 —— 于是 `downloader.py` 只需加"重试"，无需理解新语义
 14. **权威源切换要接受「个别字段本就会变」**：`#349` 的 PDF URL/体积、音频后缀都变了，
     根因是官网把该号换成了另一首诗（旧直链 404、PDF 文本可验证）；正确做法是「以站点当前为准 + 历史零删除留档」，
     而不是为了维持「0 差异」的漂亮数字去掩盖差异
 15. **CLI 入口必须写「非交互兜底」**：`--step` 因参数未解析而落进交互菜单，导致命令静默等待 17 分钟；
     除修参数解析外，再补 `sys.stdin.isatty()` 判断（无 TTY 直接退出码 2）——自动化场景的隐形杀手
 16. **长任务不要跑在工具前台终端**：块缓冲 + 终端轮换会把输出与进程一起带走；
     `setsid nohup python -u … > file 2>&1 &` + 只读轮询是本环境下唯一稳定的长任务姿势
 17. **测试的「前提」要跟实现一起复检**：断网用例因命中本地缓存而「成功」，说明缓存层已强到能顶替网络——
     把这一点补成正向用例（离线复现能力），比强行让测试回到旧假设更有价值
- 遗留事项：
  - 任务 1：`tool/` 导出脚本未含 `chorus`（如需外部导出需同步字段）
  - 任务 3/4：取证/审计脚本已按 §8 收编至 `tool/data_audit/`（含 README 与复检顺序）
  - 任务 5：P0/P1/P2 全部完成、验收全绿；可选后续优化见 `docs/API_REFACTOR_PLAN.md` §7「P3 展望」
    （如 `tool/bench_api.py` 基准回归脚本、`selenium_legacy` 的 `-m selenium` 用例、`#349` 留档目录的最终去留决策）
