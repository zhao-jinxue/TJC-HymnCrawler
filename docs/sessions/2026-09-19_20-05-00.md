# 音频时长字段 audio_durations（DB v10）+ 列序重建 + 接入全链路

- 日期：2026-09-19（会话约 20:05 起）
- 工具链：Cline
- 涉及模块：`crawler_core/db.py`、`crawler_core/audio_duration.py`（新增）、
  `tool/build_audio_durations.py`（新增）、`tool/reorder_table_columns.py`（复用）、`test/`、`tjc_hymn.db`

## 任务 1：给 tjc_hymn 增加音频时长字段并写入数据

### 提问
> tjc_hymn.db 数据库的 tjc_hymn 表中，再增加一个字段，写入每个音频的时长，参考 audio_versions 和
> audio_version_list 两个字段，一一匹配的结构，因此需要调整数据库的创建，调整 tjc_hymn.db 数据库的字段顺序，
> 需要一个音频长度统计程序，将计算好的音频长度写入数据库

### 解决思路

1. **字段结构（一一匹配怎么落）**：现有 `audio_versions`（版本名 → 本地路径）与 `audio_version_list`
   （版本名列表）是「同一批键，两种视图」。新字段 `audio_durations` 采用 **JSON：版本名 → 秒**
   （`{"鋼琴版": 144.171, "人聲版": 167.163}`），键集严格等于 `audio_versions` 的真实版本键
   （剔 `_` 前缀元信息键，用 `naming.is_audio_version_key`）——读不出时长的版本**键仍保留、值落 `null`**，
   这样「三列键集一致」可用一条 SQL / 一个统计函数直接校验，不会因个别文件缺失而塌缩。
2. **时长怎么读（两个候选，实测选优）**：
   - `ffprobe` 不存在（无 ffmpeg）；自写 MP4/MEPG 解析可行但要自己处理 VBR/Xing/坏帧，维护成本高。
   - 选 **mutagen**（新增依赖，`config/requirements.txt`）；关键实测结论：`mutagen.File()` 靠
     **类型嗅探** 选实现，对本库 3 个**无 ID3 标签**的 MP3（#138 / #197 / #296_b 人聲版）返回 None；
     改为**按后缀显式分派** `mutagen.mp3.MP3` / `mutagen.mp4.MP4` 后 **1119/1119 全部读出**
     （27.2–389.2 秒，中位 154.5）。故 `crawler_core/audio_duration.py` 用后缀分派，未知后缀才回落 `File()`。
   - 另外 3 个 m4a（#3 四部合唱版、#473/#474 鋼琴版）的自写 mvhd 扫描读不出（moov 在 mdat 之后且 box 处理有坑），
     mutagen 的 `MP4`（优先进音频轨 `mdhd`）全部正常——这也是不自己造轮子的直接理由。
3. **列顺序**：沿用项目既有机制——**代码 DDL 才是唯一权威**。把新列写进 `_create_table_v4`
   （紧跟 `audio_version_list`，在 `api_raw` 之前），再跑 `tool/reorder_table_columns.py` 事务重建表
   对齐物理列序（v5~v7 都是 `ALTER` 追加、必然错位，这次不再让 DB 落后）。
4. **谁能写这一列**：`audio_durations` **只由统计工具写**。`save_to_db`（Step 2 / 增量同步）与
   `downloader` 的路径回写都不碰它，避免爬取流程重跑时把已算好的时长清空；`save_to_db` 里也
   幂等 `ensure_audio_durations_field`（只补列、不改值）。
5. **工具形态**：`tool/build_audio_durations.py`，选项风格对齐 `tool/build_score.py`
   （`--only/--limit/--db/--dry-run/--stats/--show/--quiet`），批量统计里单个坏文件只记 `null` + 原因、
   不中断整批；默认幂等（JSON 未变的行不计入“更新”）。
6. **测试**：不引入外部音频素材——**合成** 100 帧 MPEG1 Layer3 CBR（2.61 s）与最小 MP4
   （`ftyp + moov[mvhd, trak[mdia[hdlr('soun'), mdhd]]]`，含「无 soun 轨回落 mvhd」两条路径）
   做真文件读取断言；工具逻辑用临时库 + 合成文件覆盖（键集一一匹配 / 幂等 / dry-run / 坏文件 / 子集选择）。

### 最终结果

**改动文件**
- `crawler_core/audio_duration.py`（新增）：`resolve_audio_path` / `read_duration_ex` / `read_duration` /
  `durations_for`（后缀分派 mutagen，坏文件返回原因不抛异常）
- `crawler_core/db.py`：`_create_table_v4` 增列 `audio_durations TEXT DEFAULT '{}'`（29 列）；
  新增 `ensure_audio_durations_field`（v10 幂等补列）、`save_audio_durations`、
  `AudioDurationStats`(TypedDict) + `audio_duration_stats`（覆盖/键集一致性统计）；
  `save_to_db` 只补列不动值；`print_db_status` 增「有音频时长: 474/474 ✅ | 条数 1119/1119 | 总时长 47h59m」
- `tool/build_audio_durations.py`（新增）：全量/子集统计 + 写库 + 键集校验报告 + `--stats` / `--show`
- `config/requirements.txt`：`mutagen>=1.47`
- `test/test_audio_duration.py`（新增 18 项）、`test/test_maintenance.py`（列序锚点 +1 列）、
  `test/test_jianpu.py`（28 → 29 列断言）
- `tjc_hymn.db`：`init_db()` 补列 → `tool/reorder_table_columns.py` 重建（自检 9 项全过：
  列序/列定义/474 行数据/`sqlite_sequence`=949/`hymn_category`/`integrity_check`/无空闲页）
  → `tool/build_audio_durations.py` 写入 474 行
- 文档：`README.md`（结构树 / 流水线表 / 字段表 / UPSERT 说明 / 工具用法）、`.clinerules`（工具命令 +
  类型检查事例）、`docs/SESSION_SUMMARY.md`

**验证**
- 首次全量：`待统计 474 首 → 1119 条读出 / 0 读不出 / 耗时 1.6s`，写 474 行；
  再跑一次：`更新 0 行 / 未变 474 行`（幂等），键集校验 474 行全一致 ✅
- `--stats`：已填 474/474 行、条目 1119/1119、总时长 **47h59m（172798 秒）**、键集不一致 0；
  各版本平均：鋼琴版 2:36（473）、人聲版 2:58（460）、四部合唱版 2:41（46）…
- SQL 独立复核：`json_array_length(audio_version_list) != json_each(audio_durations) 计数` → **0 行**；
  `sum(value)` = 172798.2 秒
- `--show 1`：鋼琴版 2:24（144.171）/ 人聲版 2:47（167.163）；`--show 206`：2:40 / 2:51
- 门禁：pytest **211 passed**、ruff 0、mypy（26 files）0、bandit（0 issues）0、
  pyright `--pythonpath /home/zjx/python_env/bin/python .` → **0 errors 0 warnings**
- 遇到的坑：pyright 先报 15 条——`audio_duration_stats()` 的返回是**多形状 dict**
  （计数 / 秒数 / 分版本明细），从字面量推断被收窄成 `int`，调用方 11 处全炸 →
  正是 `.clinerules`「多形状 dict 必须 `TypedDict`」那条硬规矩，改用 `AudioDurationStats` 后清零；
  另把 `read_duration_ex(None)` 的签名放宽为 `str | None`（DB 脏数据场景真实存在）。
- 遗留：无。可选增强——若日后新增 wav/ogg 等版本，`audio_duration.py` 的未知后缀会走 `mutagen.File()` 兜底。

---

## 任务 2：把音频时长接入 crawler_api 全链路（下载即入库）

### 提问
> 音频长度计算程序，是否加入了 crawler_api.py 整体的全链路程序中，即下载完新的音频，
> 自动计算音频长度，随着新音频一起写入数据库？

### 解决思路

**先如实回答：没有。** 任务 1 只做到了「新增字段 + 独立命令」——全库检索 `audio_durations` 的调用点，
`crawler_api.py` / `downloader.py` 里一处都没有；`crawler_api.py --step 11` 当时也不存在，
用户若只跑爬虫流程，新下载的音频**不会有**时长。

**接入点设计（三个缺口一次补齐）**
1. **下载即算（最关键）**：`downloader._backfill_paths_to_db()` 是下载后回写音频路径的唯一出口，
   在此**顺手读出刚下载音频的时长**，与 `audio_versions` / `audio_version_list` 写在**同一条 UPDATE**
   → 键集天然一一匹配（不用等全量脚本，也不必担心两列之间有中间态）。
   顺手把该函数里重复计算的 `SCRIPT_DIR` 改为从 `config` 导入（顺带可测）。
2. **全流程兜底**：新增 `crawler_api.run_step_audio()`，接到「全流程（菜单 7）」与
   Step 10 全量/增量同步末尾——覆盖「路径来自 probe 回填 / 历史数据 / 手工放进目录」等
   未走下载回写的场景；幂等（未变行只报告）。
3. **独立入口**：菜单新增 `11`、CLI 新增 `--step 11`（别名 `--step audio`），
   提示语与 `--step` 帮助同步到 `1-11`。

**避免三处规则漂移**：把批量统计/写库逻辑上收到 `audio_duration.fill_durations()`
（`DurationFillSummary` 用 TypedDict 显式声明），tool 与 crawler_api 都调它——
「键集一一匹配」的规则只有一处实现，`tool/build_audio_durations.py` 退化为薄 CLI（保留原参数与输出）。

### 最终结果

**改动文件**
- `crawler_core/downloader.py`：`_backfill_paths_to_db` 增 `audio_durations` 同批写入 + 时长读出计数打印
- `crawler_core/audio_duration.py`：新增 `fill_durations`（批量）/ `load_version_rows` / `durations_of` /
  `_version_set` 与 `DurationFillSummary`(TypedDict)；`durations_for` 统一保留 3 位小数
- `crawler_api.py`：新增 `run_step_audio()` 与 `print_audio_duration_summary()`；接入菜单 `11`、
  `--step 11|audio`、全流程（`_dispatch` 的 7）、`run_step10_full` / `run_step10_incremental` 末尾；
  提示语与帮助文本更新为 `1-11`
- `tool/build_audio_durations.py`：改为复用 core 的批量实现（参数/输出不变）
- `test/test_audio_duration.py`：+6 项（`fill_durations` 写库/幂等/dry-run/子集；
  下载回写三列同批写入；crawler_api 步骤委派、`--step 11|audio`、全流程与菜单 7 末尾调用）
- 文档：`README.md`（运行示例、菜单表、全链路说明、流水线表、工具用法）、`.clinerules`、`docs/SESSION_SUMMARY.md`

**验证**
- pytest **217 passed**；ruff 0；mypy（26 files）0；pyright **0 errors 0 warnings**（bandit 0 issues）
- `python crawler_api.py --step 11`（真库）：
  `🔊 474 首 / 1119 条音频（读出 1119，读不出 0）| 写入 0 行 / 未变 474 行（库内共 474 首，本次处理 474 首）`
- **真库回写实测**：直接以真 `probe_report.json` 调用 `downloader._backfill_paths_to_db` →
  `📌 数据库已更新：五线谱 474 | 简谱 474 | 音频 474` + `🔊 音频时长已随音频一起入库（audio_durations）：读出 1119 条`；
  回写前后对三列（`audio_versions` / `audio_version_list` / `audio_durations`）逐行快照 **diff 为空**（幂等），
  `json_array_length(audio_version_list) != json_array_length(audio_durations)` 计数 **0**
- 独立工具仍可用：`python tool/build_audio_durations.py --quiet` → 474 首 / 1119 条 / 未变 474 行
- 遇到的坑：`fill_durations` 里留了个未使用的 `total` 变量被 ruff 抓出（F841）→ 改为写进
  `DurationFillSummary.rows/selected`，顺带让 `crawler_api` 的摘要能显示「库内共 N 首，本次处理 M 首」
- 遗留：无

---

## 会话总结

- 本次共处理 2 个任务
- 关键收获：① 音频时长读取**不能信 `mutagen.File()` 的类型嗅探**（无 ID3 的 MP3 会返回 None），
  按后缀显式分派 `MP3`/`MP4` 后本库 1119/1119 全读出；② 新增字段/统计的正确落地顺序是
  「代码 DDL → `reorder_table_columns.py` 重建列序 → 统计工具回填 → **接入下载回写与全流程兜底**」，
  只在工具里做=没接入；③ 同一业务规则（键集一一匹配）必须只有一处实现
  （`fill_durations`），否则 crawler_api / downloader / tool 三处早晚漂移
- 遗留事项：无（`audio_durations` 覆盖 474/474，且下载即写、全流程兜底、`--step 11` 可重算）
