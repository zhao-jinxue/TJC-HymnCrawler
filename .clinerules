# hymn_crawler 项目规则

## 新会话启动流程（续接开发，目的：最小化上下文）
按以下顺序加载上下文，不要多做任何事：

1. 读 `docs/SESSION_SUMMARY.md`（开发总结）——按需全文或关键章节
2. 若存在 `docs/UI_CONFIRMATION.md`，只读「最终设计定稿」章节（用 start_line/end_line 限定）
3. 执行 `git log --oneline -15` 查看提交历史
4. 用不超过 5 行汇报：当前状态 / 最近提交 / 遗留任务，然后停止等待用户指令

**禁止**：
- 不读 `.clinerules` / `.mcp.json` / 全局规则（系统已自动注入）
- 不遍历用户配置目录、不扫描整个仓库目录树
- 不调用任何 MCP 工具
- 不整文件读取长文档，优先按章节读取

## 项目关键信息
- 项目类型：TJC 赞美诗（hymn）数据爬虫 + 数据处理流水线
- **目录约定（2026-09-13 重排）**：项目根只保留 `README.md` / `LICENSE` / `crawler_api.py` / `tjc_hymn.db`；
  依赖与门禁配置在 `config/`，产物在 `data/`，保底入口在 `legacy/`，资源在 `Hymn_Downloads/`（详见 README 项目结构）
- Python 环境：`/home/zjx/python_env/bin/python`（内含 opencc / rapidocr 等依赖）
- 核心代码：`crawler_core/`（爬虫框架）、`tool/`（数据处理工具）、`test/`（测试）
- 主入口：`crawler_api.py`（原 `crawler_fast.py`）；保底入口：`legacy/crawler_selenium.py`
- 数据文件：`tjc_hymn.db`（SQLite 数据库：v10 主表 29 列，含音频时长 `audio_durations`；另有 v8/v9 简谱七表）、
  `data/probe_report.json`、`data/final_report.txt`
- 测试命令：`/home/zjx/python_env/bin/python -m pytest -c config/pytest.ini test/ -x -q`
- 门禁命令：`ruff check --config config/ruff.toml .`｜`mypy crawler_core crawler_api.py legacy/crawler_selenium.py`｜
  `bandit -c config/bandit.yaml -r crawler_core crawler_api.py legacy/crawler_selenium.py`｜
  VSCode/Pylance 对齐检查：`npx pyright --pythonpath /home/zjx/python_env/bin/python .`
  （**必须带 `--pythonpath`**：CLI 不会自动识别名为 `python_env` 的解释器，也不读 `.vscode/settings.json`，
  否则 selenium/PIL/bs4 会被误报为 `reportMissingImports`——19 条假告警。
  **基线：全项目 0 errors**，2026-09-16 从 73 errors 清零）
- **类型检查约定（2026-09-16 血泪）**：Pylance/pyright 的"推断过窄"会在别处炸出几十条假报错，两条硬规矩：
  1. **公共函数的参数与返回必须显式注解**——未注解参数会被推成 `Unknown`，
     `dict.get(k, k)` 这类会退化成 `str | None`（`normalize_sym` 曾因此让 `grade_of` 误报可迭代 None）
  2. **返回"多形状 dict"必须用 `TypedDict` 声明**——如 `db.load_score()` 的 `{hymn, lines, lyrics, chars}`；
     若从字面量推断会被收窄成 `dict[Literal["hymn"], dict[主行列, Any]]`，
     于是调用方 `rec["lines"]` 全被误报（`tool/build_score.py` + `tool/show_score.py` 曾一次炸出 70 条）；
     `db.audio_duration_stats()`（计数 / 秒数 / 分版本明细三种形状）用 `AudioDurationStats` 声明，同理
- 歌词/副歌复核：`python tool/show_lyrics.py <编号|区间>`（`--stats` 看覆盖率、`--no-chorus` 列无副歌编号）
- 带简谱歌词复核：`python tool/extract_jianpu.py [--dry-run] [--only 1 5 349]`（PPT→两表+报告）、
  `python tool/show_jianpu.py <编号> [--map]`（简谱字体渲染出图 + 逐字对位索引）、`--review` 列被标记的行
- 官方简谱曲谱（v9 五表）：`python tool/build_score.py [--learn] [--learn-mode anchors|row] [--limit N] [--dry-run]`
  （官方 PDF→入库；`--learn` = **拍位级锚点投票**学「码位→记号」（默认 `anchors`，旧整行同构用 `row`），
  幂等可重跑——会先清掉上一轮同来源映射再学）、
  `--show <编号>`（打印曲谱/歌词/逐字对应）、`--stats`（等长行 / 逐字可靠率）；
  表名 `hymn_score` / `hymn_score_line` / `hymn_score_lyric` / `hymn_score_char` / `hymn_codepoint_map`
- 曲谱 + 歌词人读输出：`python tool/show_score.py <编号|区间>`（谱行 × 词行**竖排对齐**——曲谱与它配的歌词
  按列对齐打印，可直接贴文档）；`--chars` 逐字明细、`--all-parts` 含和声部、`--list` 列库内编号；
  **按节分页打印**（唱诗用）：`--by-stanza`（每页 = 全部谱行 + 该节词行；副歌行每页重复、标签 `词ⓒ`；
  副歌在曲谱里没有词行时每页页尾附官网 `chorus` 文本）、`--out FILE` 写文件、`--formfeed` 出换页符 `\f`
- 音频时长（v10 字段 `audio_durations`，JSON「版本名 → 秒」与 `audio_versions` / `audio_version_list`
  **键集一一匹配**；读不出的版本值落 `null`）：`python tool/build_audio_durations.py`
  （离线读本地 m4a/mp3 时长；幂等可重跑，重下/换音频后需重跑）、`--only 1 5 349` / `--limit N` 子集、
  `--dry-run` 只统计不写库、`--stats` 覆盖统计、`--show 编号` 打印单首（`mm:ss` + 秒数 + 文件是否存在）；
  读取实现 `crawler_core/audio_duration.py`（mutagen **按后缀显式分派** `MP3`/`MP4`，
  因为 `mutagen.File()` 嗅探对无 ID3 标签的 MP3 会失败——本库 #138/#197/#296_b 即此例）
- 构建/验证：参考 `docs/hymn_crawler_plan.md`（历史计划）与 `docs/CLINE_CONTEXT_MINIMIZE.md`

## 千问 API Key（图片解析/理解）
- **触发条件**：任务涉及"解析图片、理解图片、识别图片内容、读取图片文字、视觉问答"时，使用千问多模态视觉 API
- **API Key 存放**：本地文件 `/home/zjx/.cline/qwen_key.txt`（脚本读取，**禁止**明文写入代码 / 规则 / 文档）
- **调用方式**：OpenAI SDK 兼容格式，base URL `https://dashscope.aliyuncs.com/compatible-mode/v1`，模型 `qwen-vl-ocr`（目录 OCR）/ `qwen-vl-plus` / `qwen-vl-max`
- **现有脚本**：`tool/qwen_ocr.py`（调用 Qwen-VL 识别目录图片中的文字，key 从本地文件读取不打印），模型可在 `--model` 参数覆盖

## 会话日志强制流程（Cline 开发记录档案）
> 目的：将使用 Cline Agent 的开发经历完整留档——每个会话的提问、解决思路、最终结果，形成可回溯的项目档案。

### 强制流程（每个任务必须遵守）
1. **任务开始（会话内第一个任务时）**：在 `docs/sessions/` 目录以**会话开始时间**创建日志，命名为 `YYYY-MM-DD_HH-MM-SS.md`，复制 `docs/sessions/_template.md` 为基底
2. **记录提问**：记录用户的初始提问与后续追问（保持用户原意，技术细节可简写）
3. **记录思路**：动手前先阐述分析结论、可选方案对比、选定方案与理由
4. **记录结果**：任务完成或中断时记录——完成结论、关键改动文件清单、验证方式、遇到的困难、遗留问题
5. **日志章节**（模板已固化，必须包含）：`## 提问` / `## 解决思路` / `## 最终结果`
6. **多任务处理**：一个会话可能包含多个任务——同一日志文件内按 `## 任务 N：<标题>` 分段追加，全部任务完成后再写 `## 会话总结`
7. **与 git 同步**：日志随代码改动一起 `git add docs/sessions/` 提交

### 质量要求
- 只记录**有价值的决策与结论**，禁止工具调用流水账
- 结果必须含三要素：**改了什么文件 / 验证了什么 / 遗留什么**
- 中断的会话也要记录已完成步骤与未完成事项（以便续接）
- 规范细节见 `docs/sessions/README.md`；模板见 `docs/sessions/_template.md`

## Git 提交与推送（每个任务完成后的强制流程）
> 目的：保证每次问题处理完成后，代码与文档改动及时提交并推送，避免堆积与丢失。

### 强制流程
1. **任务完成后**：检查 `git status`，确认本次改动的文件清单（代码 / 文档 / 会话日志等）
2. **写清提交信息**：`git add` 相关文件后提交，提交信息用中文、简洁描述改动内容（多处改动可用「；」分隔）
3. **立即推送**：提交成功后执行 `git push` 推送到远程
4. **变更 .clinerules 本身**时：提交信息标注「项目规则：...」，随规则改动一并提交推送

### 禁止
- 禁止将修改堆积到多个任务后才统一提交（除非用户明确要求合并提交）
- 禁止提交包含无关文件的改动（仅提交与当前任务相关的文件）
- 禁止在提交信息中使用无意义的描述（如 update / fix）

### 大文件防线（2026-08-18 加入）
> 背景：曾误将 45MB `Clash.Verge_*.exe` 安装包提交，导致 git commit 压缩大对象耗时数十秒、Cline 会话长时间无响应。

1. **禁止提交 >5MB 的二进制文件**（exe/msi/dmg/zip/tar.gz/7z 等）——`.gitignore` 已默认忽略，新增此类文件的提交请求一律拒绝
2. 大型资源（音频 / PDF / 安装包 / 图片）一律放入 `Hymn_Downloads/`（已被 gitignore 忽略）或项目外目录，仅通过 README 记录获取方式
3. **提交前自查**：`git status` 若出现陌生大文件（`*.exe` 等），先移除出暂存区再提交
4. **大文件已入库时**：优先重做提交（`git rm --cached <file>` → `git commit --amend` / `git reset --soft` 重提），避免历史膨胀；重提后 **立即清残留对象**：`git reflog expire --expire=now --all && git gc --prune=now`（否则 45MB blob 以不可达对象永久残留、`.git` 持续膨胀）
5. **禁止禁用 git 自动维护**：不得设置 `gc.auto=0`（曾因该配置导致松散对象从不打包、60MB `.git` 卡顿）；需关闭自动 gc 时用 `git config gc.auto 0` 仅在临时场景使用，事后必须恢复（`git config --unset gc.auto`）
6. **推送超时**：GitHub 走 SSH（`git@github.com` 已配置），推送前确认本地远程差异少量（`git status -sb`），避免大提交推送中断

## 文档防污染（2026-08-18 加入）
> 背景：`.clinerules` 与 `docs/sessions/` 日志曾混入工具调用的 XML 残留（如 `</｜｜DSML｜｜>`、`</replace_in_file>`、`<task_progress>` 等），污染文档内容。

### 成因（避免重蹈覆辙）
- 在**会话中断 / 任务恢复**场景下，工具调用可能被系统重试；若调用参数内误包含工具自身的闭合标签，这些标签会被当作**文件正文**写入目标文件。

### 强制规则
1. **写文档类文件时**：`write_to_file` / `replace_in_file` 的正文内容中**严禁出现**工具标签（如 `<task_progress>`、`</｜｜DSML｜｜>`、`</replace_in_file>`、`</tool>`等）——正文只能有 Markdown 文本
2. **工具调用收尾自查**：每次 `replace_in_file` / `write_to_file` 后，若该文件是 `.md` / `.rules` 等文本文档，应快速用 `read_file` 核对文件末尾是否有异常标签残留；发现后立即清理再提交
3. **文件被污染时的处理**：直接重写整个文件（`write_to_file`），或删除污染行（`replace_in_file`），严禁带着垃圾标记提交
4. **发现即报告**：若在 `.clinerules` / 会话日志中发现工具标签残留，应主动记录到会话日志并在本次提交中一并修复
